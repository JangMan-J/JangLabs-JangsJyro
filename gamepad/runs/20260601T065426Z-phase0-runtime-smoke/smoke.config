# Phase 0a runtime smoke — keyboard/mouse output only (Linux virtual-gamepad output is stubbed).
# S  = South face button -> SPACE key
# ZR = right trigger full pull -> left mouse button
RESET_MAPPINGS
S = SPACE
ZR = LMOUSE
