---
context: default
phase: gamepad-lab Phase 4 (mapper-conversion lab; no GSD phase scaffolding — lab-native plan governs)
task: batch 2a (slot-order matrix) DONE + lead-gated; batch 2b (L2/L3/ramps, task #9) next — needs fresh Steam clearance
total_tasks: see "remaining" below
status: paused
last_updated: 2026-06-12T17:30Z (session 8: gated+ingested+committed session-7 matrix run; autosave restored)
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: steam-client-user-first — ONE Steam instance per box and the USER owns it.
      Mitigation: obtain explicit user clearance BEFORE any nested-env spin-up (`steam-virtual-env.sh`).
- [ ] CONSTRAINT: task-description-precedence — a teammate followed a stale task description over a
      newer lead correction (two wasted gate rounds). Mitigation: spec changes are made by AMENDING
      the task record (TaskUpdate); messages are notifications only. On conflict, teammate must stop and ask.
- [ ] CONSTRAINT: stimulus-confirmation — `press BTN_SOUTH` is invalid control-FIFO DSL (short names:
      `press SOUTH`); it logs WARN and injects NOTHING. A whole session of "output silence" was
      stimulus-never-sent. Mitigation: on ANY silence, check holder log/evdev BEFORE interpreting.
- [ ] CONSTRAINT: raw-layer-only — XI2 key-layer (Device) events are delivery-queue flush artifacts;
      `xinput test-xi2` synthesizes the final release at client exit. ONLY Raw* timestamps are real.
      Mitigation: normalizer enforces raw-first (committed f5655dd); never hand-analyze key-layer timing.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| stale-spec-over-message | Teammate implements original task text, ignoring newer lead ruling | blocking | Amend task records for every spec change; require item-by-item compliance reports against the task description |
| silence-misread | Output silence interpreted as suppression/SI-off when stimulus never injected | blocking | Amended canary rule in findings/steam_lane_behavior.md §operational gotchas |
| key-layer-timing | Trusting XI2 Device-event timestamps | blocking | Raw-first normalizer + regression tests vs p2-120ms-raw-audit.jsonl |
| autosave-as-current-state | Reading the on-disk desktop-layout vdf as live state | advisory | Disk is stale BOTH directions while Steam runs; tonight it did not flush even on client EXIT (no config writes at all observed) |
| pgrep/pkill -f self-match; find without -L through ~/.steam symlink | Shell traps that produced confusing nulls this session | advisory | Use -x or exact PIDs; use `find -L` |

<current_state>
JangLabs/jangsjyro `branch-a-port`, gamepad/ lab, mapper-conversion project. Session 6 ended after a
marathon: Phase 4 batch 1 + 1b complete and lead-gated; all three oracle double-press predictions
PINNED with statistics; the Release_Press investigation escalated into the discovery of a
STATE-DEPENDENT activator-loss Heisenbug in Steam itself (order-dependence falsified; rebinding/ghost
state implicated; per-controller config cache is prime suspect). Slot-order permutation matrix
layouts are BUILT and committed, awaiting live runs. Working tree CLEAN: 18 commits this session,
HEAD = cb96f46. Team `jsmlab` (builder/runner) shut down cleanly at pause. Nested env fully torn
down; Steam returned to user and now closed. CEF debug flag left in place.
</current_state>

<completed_work>
- Gifts ingested: Steam Input guide 2804823261 (reference/steam-input-guide/), steam-console CDP
  bridge capability recorded, grandfather-Claude's pygame tool upgraded to app-plane keystroke
  logger (D2D/R2D/R2R; KDE-Wayland stop_text_input fix) — lives OUTSIDE repo at
  ~/_project.bak/.research_backup/tools/inventory_input_test.py, adoption candidate.
- Oracle model: d2d epoch attested → traced → pinned; re-send axiom retracted AND falsified;
  down-anchored timer axiom recorded (findings/steam_lane_behavior.md §Oracle model).
- Phase 4 assets (3491aa6): marker layout (4 activators, one button, in-stream Start/Release
  markers), remove_layer + action_set_swap on verified `controller_action` binding strings
  (numeric preset ids; change_preset verb = flagged hypothesis), 5 pin-trace DSL scripts.
- Batch 1+1b (e97e495, 32789a2): predictions pinned (singles 192.23±0.84 ms anchor, N=14; double
  at second-down ~0 ms; held-double clean); XI2 raw-vs-key delivery model; spew = dead end for
  activator tracing; SI activation = identity+enumeration-keyed (single-pad-first protocol).
- Normalizer raw-first fix + zero-duration pair guard (f5655dd, 4adeedf) — known-answer table
  unchanged (no committed verdict was exposed).
- Slot-order permutation matrix layouts, 6 files (b52b82f), emitter round-trip order-preserving.
- Product doctrine recorded (6a7eecb): "human food" — ~10–15 ms timing equivalence; DTT-edge
  inclusivity ruled MOOT; epoch differences stay relevant.
- Oracle GUI evidence archived (reference/oracle-gui-observations/, 3 screenshots): slot model,
  ghost slot indices, same-order-now-works.
</completed_work>

<remaining_work>
- ~~Batch 2a slot-order matrix~~ DONE (session 7, run 20260612T072850Z-phase4-batch2-matrix;
  lead-gated session 8): Release absent ALL 16 cells/all orders (GUI order effect NOT reproduced);
  Start always fires; NEW: Set B Double fully suppressed under Start+Release co-residency.
  Per-cell state controls + cache hunt DROPPED per environment-trust doctrine (71da330).
- Batch 2b (task #9, NEEDS FRESH USER CLEARANCE on Steam client):
  L2 remove_layer live verification (binding strings statically verified, never fired live);
  L3 change_preset falsification probe; RZ=200 trigger ramp set (script exists, never run).
- min_activation_time pin (~34 ms quick-tap width ≈ controller_min_activation_time cvar — INFERRED,
  unpinned; could pin by varying the cvar via steam-console live).
- Harvest the desktop-layout autosave IF a flush ever lands (tonight: no config writes even on
  Steam exit — flush trigger unknown; the ghost-slot serialization remains unobserved).
- Phase 5 orchestration; Phase 8 KB seeding from pinned rules; Phase 6 gyro still uhid-gated.
- Possible upstreaming: file the activator-loss Heisenbug to Valve once the matrix characterizes it.
</remaining_work>

<decisions_made>
- Human-food tolerance doctrine (user): comparator/classification tolerances calibrate to human
  discernibility (~10–15 ms); no traces on sub-perception questions.
- Converter hazard doctrine: Start/Release + Long/Double combos = state-dependent activator loss;
  classify ≤ degraded_approximation, never emit such combos, flag on input (anti-Goodhart: don't
  convert INTO a probable Steam bug).
- Raw-layer-only doctrine for all XI2 timing (see constraints).
- Steam scheduling: user-first, always ask before env spin-up.
- Team protocol: task records are the single source of spec truth; lead re-runs every gate.
</decisions_made>

<blockers>
- Steam client availability: user-owned; batch 2 blocked on their go-ahead (not a technical blocker).
- Ghost-slot serialization evidence: blocked on Steam ever flushing the autosave (trigger unknown).
- Phase 6 gyro: root uhid setup (udev rule / ACL) — needs user/root, unchanged.
</blockers>

## Required Reading (in order)
1. `gamepad/mapper-conversion-lab-plan.md` — "▶ NEXT SESSION — START HERE" banner (consolidated session-6 save-state) + §14 roles
2. `gamepad/findings/steam_lane_behavior.md` — §Oracle model, §XI2 delivery model, §Order-dependent edge-activator loss (the Heisenbug), §operational gotchas (canary rule, SI activation, autosave staleness)
3. `gamepad/reference/phase4-layouts/README.md` — matrix layouts and what each pins
4. `gamepad/reference/oracle-gui-observations/README.md` — the three-screenshot Heisenbug narrative
5. `gamepad/CLAUDE.md` — lab conventions (where things go, claim strengths)

## Infrastructure State
- Nested env: DOWN (verified). Synthetic pad: none. Steam: closed by user, theirs to launch.
- CEF debug flag: PRESENT (~/.steam/steam/.cef-enable-remote-debugging) — steam-console works
  against any future Steam session; mild localhost attack surface, user-approved.
- Team jsmlab: shutdown requested at pause (re-form via TeamCreate + Agent(model:sonnet) per §14).
- Task list (~/.claude/tasks/jsmlab/): all tasks closed.
- Autosave 413080 slot: restored to pre-batch backup (verified byte-identical earlier).

<context>
The lab crossed from "characterize documented mechanics" into "we found an undocumented
state-dependent bug in Steam Input and built the instrument to dissect it." The user (oracle) is
deeply engaged — their GUI sessions produced the decisive falsifications. Keep consulting them
before Steam-lane loops; keep the lead-gate discipline (it caught real spec failures every round);
keep everything committed at gates. Billing/model discipline: lead on the expensive model, all
iteration on Sonnet teammates.
</context>

<next_action>
Start with: read the plan banner (session-8 save-state), then ask the user for a fresh Steam
clearance window for batch 2b (task #9: L2 remove_layer / L3 change_preset / RZ=200 ramps).
On clearance, re-form team jsmlab and dispatch per §14; task #10 (teardown) follows. The
session-7 matrix is fully gated/ingested/committed — do not re-run it.
</next_action>
